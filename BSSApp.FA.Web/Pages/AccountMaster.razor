@page "/accountmaster"
@inherits AccountMasterBase

@*<h3>Account Master</h3>*@
<div class="row">
    @*<div class="col-sm-8">
        <div class="card">
            <div class="card-header">
                <h1>Account Master</h1>
            </div>
            <div class="card-body">
               
                <div class="col-8">
                    <EditForm Model="@AcMasters">
                        <div class="form-group">
                            Select Ledger
                            <InputSelect id="ldgr" placeholder="Select" class="form-control" @bind-Value="LedgerID">
                                @foreach (var ledger in Ledgers)
                                {
                                    <option value="@ledger.LedgerID">@ledger.LedgerCode @ledger.LedgerName</option>
                                }
                            </InputSelect>
                        </div>
                        @if (AccountGroupMasters == null)
                        {
                            <div class="spinner"></div>
                        }
                        else
                        {
                            <div class="form-group">
                                Account Groups
                                <select id="accountgroup" class="form-control" @onchange="OnAccountGroupChange">
                                    <option>Select account group</option>
                                    @foreach (var accountgroup in AccountGroupMasters)
                                    {
                                        <option value="@accountgroup.AccountGroupCode">@accountgroup.AccountGroupCode @accountgroup.AccountGroupName (@accountgroup.LedgerCode)</option>
                                    }
                                </select>
                            </div>
                        }
                    <div class="form-inline">
                        <a class="text-danger">Account Code (New) :</a>
                        <input type="text" id="txtaccountcode" value="@maxaccountno" class="form-control" />
                    </div>
                    <div class="form-group">
                        Acoount Name
                        <input id="txtaccountname" class="form-control" />
                    </div>
                    <div class="form-group">
                        S/o, W/o, D/o
                        <input id="Acad1" class="form-control" />
                    </div>
                    </EditForm>
                </div>
                
                <p>The content for account master</p>
                @if (AcMasters == null)
                {
                    <div class="spinner"></div>
                }
                else
                {
                    @foreach (var e in AcMasters)
                    {
                        <h2>@e.LedgerCode  @e.Ledger.LedgerName @e.Acno</h2>
                    }
                }
            </div>
        </div>
    </div>*@
    <div class="col-sm-8">
        <div class="card">
            <div class="card-header btn-gradian1 text-white-50 text-center">
                <h1>Account Master Actions Detail</h1>
            </div>
            <div class="card-body text-center">
                <a href="/findaccount" class="btn btn-outline-info btn-width-accountMaster">Find Account</a>
                <hr />
                <a href="/addaccount" class="btn btn-outline-info btn-width-accountMaster">Add New Account</a>
                @*<hr />
                <a href="#" class="btn btn-primary">Update</a>
                <hr />
                <a href="#" class="btn btn-primary">Delete</a>*@
            </div>
        </div>
    </div>
</div>

